spring:
  data:
    mongodb:
      port: 0 # when flapdoodle using
      database: books-test
  shell:
    interactive:
      enabled: false
    command:
      version:
        enabled: false
  aop:
    proxy-target-class: false

de:
  flapdoodle:
    mongodb:
      embedded:
        version: 6.0.11

mongock:
  enabled: false
